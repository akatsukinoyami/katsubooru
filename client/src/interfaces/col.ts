import type { columnSizeType } from "../types";

export default interface ColInterface {
  className?: string;
  s?: columnSizeType;
  m?: columnSizeType;
  l?: columnSizeType;
  xl?:columnSizeType;
}
